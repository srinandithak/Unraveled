export function NoiseOverlay() {
    return (
        <>
            <div className="noise-overlay" aria-hidden="true" />
            {/* 
        The woven texture is applied contextually to specific sections per the prompt, 
        so we just provide the global noise here. Woven texture is a utility class `texture-woven`.
      */}
        </>
    );
}
